<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        .cont {
            width: 40%;
            padding: 2rem;
            margin: 6.8rem auto 0;
            border-radius: .8rem;
            box-shadow: 3px 10px 20px rgb(0, 0, 0, 0.2)
        }
        .row {margin: 0}
        @media (max-width: 600px) {
            .cont {
            width: 80%;
            margin-top: 15rem;
            padding: 2rem 1rem;
            }
            .sm-size {
                height: 3rem !important;
            }
        }
    </style>
    <title><%= pageName %></title>
</head>
<body>
    <div class="row">
        <div class="img">
            
        </div>
        <div class="cont col-sm-12">
            <div class="title text-center mb-4"><h1><%= pageName %></h1></div>
            <form action="/auth/<%= pageRoute %>" method="POST">
                <% if (pageRoute !== 'login') { %>
                <div class="mb-3">
                    <label class="form-label">Username</label>
                    <input type="text" class="sm-size form-control" name="username" required>
                </div>
                <% } %>
                <div class="mb-3">
                    <label for="exampleInputEmail1" class="form-label">Email address</label>
                    <input type="email" class="sm-size form-control" name="email" required>
                </div>
                <div class="">
                    <label for="exampleInputPassword1" class="form-label">Password</label>
                    <input type="password" class="sm-size form-control" name="password" required>
                </div>
                <div id="passwordHelp" class="mb-3 form-text"><%= passMessage %></div>
                <button type="submit" class="btn btn-primary w-100 sm-size">Login</button>
            </form>

        </div>
    </div>
</body>
</html>